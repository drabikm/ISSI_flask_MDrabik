Application written in Flask for displaying movie list, with possibility to add new ones and delete old ones

In order to install and use the application, you need to install requirements from requirements,txt file. To do so, use this command: pip install -r requirements.txt
